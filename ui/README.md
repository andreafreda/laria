# ui

LARIA web interface — **Angular** SPA. Talks to the core over a REST/WebSocket API
(real-time). It is the native dashboard and the source of truth for interactive views
(sliders, drill-down, charts) that on HARIA today are Lovelace-side hacks.

Bootstrap TODO: scaffold the Angular project + auth + API client.
