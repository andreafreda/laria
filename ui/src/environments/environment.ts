// Replaced by environment.prod.ts in production builds (see angular.json).
// In development the API runs separately (python -m laria.web) on :8080.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
};
