"""Messaging channels: ways to reach the engine besides the web API.

Each channel adapts an external messaging system (Telegram, ...) to the same
engine. Run one as its own process, e.g. ``python -m laria.channels.telegram``.
"""
